<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Menu.ViewModels"
             x:Class="Menu.CartPage"
             Title="CartPage">
    <ScrollView>
        <StackLayout Padding="10">
            <Label Text="Your Cart" FontSize="30" HorizontalOptions="Center" />

            <!-- Display Cart Items -->
            <CollectionView ItemsSource="{Binding CartItems}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="5">
                            <Label Text="{Binding Title}" />
                            <Label Text="{Binding Price, StringFormat='{}{0:C}'}" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Display Total -->
            <Label Text="{Binding Total, StringFormat='Total: {0:C}'}" FontSize="20" HorizontalOptions="Center" />
            <Button Text="Return to Home"
                Command="{Binding NavigateHomeCommand}"
                HorizontalOptions="Center"
                VerticalOptions="EndAndExpand" />


        </StackLayout>
    </ScrollView>
</ContentPage>